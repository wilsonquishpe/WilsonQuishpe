<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="WilsonQuishpe.Resumen"
             Title="Resumen">
    <ContentPage.Content>
        <StackLayout>
            <Label HorizontalOptions="Start" x:Name="lblEstudiante" FontSize="20" TextColor="Blue"/>
            <Grid Padding="10,0,10,0">

                <Grid.RowDefinitions>
                    <RowDefinition></RowDefinition>
                    <RowDefinition></RowDefinition>
                    
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                </Grid.ColumnDefinitions>
              
                <Label Grid.Row="0" Grid.Column="0" Text="Nombre"  HorizontalOptions="Start" VerticalOptions="Center" />
                <Label Grid.Row="0" Grid.Column="1" x:Name="lblNombre"  HorizontalOptions="Start" VerticalOptions="Center" />
                <Label Grid.Row="1" Grid.Column="0" Text="Total"  HorizontalOptions="Start" VerticalOptions="Center" />
                <Label Grid.Row="1" Grid.Column="1" x:Name="lblTotal"  HorizontalOptions="Start" VerticalOptions="Center" />
            </Grid>

        </StackLayout>
    </ContentPage.Content>
</ContentPage>